# V8 pre-result freeze

This marker records that the V8 generality protocol, frozen V6 candidate, abstract-world generator, comparator registry, metrics, ecological-inference estimators, finite-budget formulas, tests and CI workflow were committed before the first V8 result inspection.

## Frozen for V8 interpretation

- candidate: exploration=0.50, evidence=0.10, observability=0.40, disagreement=0.00;
- 864-regime factorial grid in `benchmarks/v8_generality_protocol.json`;
- six-policy registry including four same-alpha comparators;
- 800 windows × 10 paired replicates per regime;
- true-event recall, evidence-hidden-error recall, disturbance-family TV and joint recovery ratio;
- naive selected-sample versus exploration-only prevalence bias/RMSE;
- no-tuning interpretation rules in `docs/V8_GENERALITY_BENCHMARK_PROTOCOL.md`.

V8 is an applicability characterization. Any future alternative weight vector or revised world generator must receive a new generation label; it must not replace the V8 result after inspection.

V7 remains independent and blocked-safe. No V7 seed, pixel artifact, trace or result is used by V8.
