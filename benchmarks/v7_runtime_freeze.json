{
  "schema": "pollipi-insepi-v7-runtime-freeze-v1",
  "status": "pre-materialisation-frozen",
  "python_version": "3.11.16",
  "numpy_version": "2.4.6",
  "capture_path": ".v7/run/v7_runtime_environment.json",
  "pip_freeze_path": ".v7/run/v7_pip_freeze.txt",
  "capture_timing": "After exact frozen observer checkouts and PolliPi dependency installation, but before either exact observer smoke test, before master-seed derivation and before V7 pixel materialisation.",
  "implementation_last_change_commits": {
    "scripts/v7_capture_runtime_environment.py": "c00cf1af17662db233089032aa965786ecadea5b",
    "scripts/v7_evaluate_locked.py": "7d6e337105577ff1f7c07987310b214a71601dff"
  },
  "execution_file_git_blob_sha1": {
    "scripts/v7_run_pollipi_frozen.py": "80942036e2683f9ae16208da718f86b3579c32b4",
    "scripts/v7_run_insepi_frozen.py": "537d1e331f3dca7f5d886500e1ceb482c408f6eb"
  },
  "insepi_frame_index_gate": {
    "probe_indices": [0, 1, 179],
    "timing": "exact V5 smoke test before V7 materialisation",
    "required_result": "All decision-relevant observation fields, local-structure loss and final risk/state/action outputs are exactly identical on fixed smoke pixels across all probe indices.",
    "failure_action": "Block V7 before master-seed derivation and pixel materialisation; do not alter the frozen observer or substitute a constant index within the same generation."
  },
  "required_pre_capture_absence": [
    ".v7/run/v7_pixels.npz",
    ".v7/run/v7_pixels_manifest.json",
    ".v7/run/v7_materialisation_receipt.json",
    ".v7/run/pollipi_v7_trace.jsonl",
    ".v7/run/insepi_v7_trace.jsonl",
    ".v7/run/v7_report.json",
    ".v7/run/v7_execution_ledger.json"
  ],
  "required_runtime_assertions": [
    "CPython version equals 3.11.16 exactly",
    "NumPy version equals 2.4.6 exactly after installing the exact PolliPi runtime",
    "pip freeze --all is captured before observer output",
    "runtime manifest records master_seed_derived=false",
    "runtime manifest records v7_pixels_materialised=false",
    "runtime manifest records observer_output_inspected=false"
  ],
  "ledger_provenance": [
    "runtime environment SHA-256",
    "runtime pip-freeze SHA-256",
    "runtime freeze SHA-256",
    "runtime Python version",
    "runtime NumPy version",
    "orchestrator git SHA already recorded by V7 execution ledger"
  ],
  "scientific_non_interference": [
    "This freeze changes no V7 generator, allocator, evaluator, materialiser, baseline, metric, threshold, policy or claim rule.",
    "No V7 master seed has been derived by adding this freeze.",
    "No V7 pixel artifact has been materialised by adding this freeze."
  ]
}
